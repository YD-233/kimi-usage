{
  "name": "kimi-usage",
  "version": "1.2.2",
  "description": "把每轮 token 用量与缓存命中率显示在 TUI 底部状态栏；kimi-code < 0.30.0 或不支持状态栏的终端仍回退到标题栏",
  "interface": {
    "displayName": "Kimi 用量统计",
    "shortDescription": "每轮 token / 缓存命中统计，默认显示于 TUI 底部状态栏"
  },
  "hooks": [
    {
      "event": "Stop",
      "command": "python3 ./scripts/usage.py || python ./scripts/usage.py",
      "timeout": 10
    }
  ]
}
